Message_URI_MProcess  (&4E382)
#Parent Wimp:Msg_
+ 20	<flags=>URI_processflags>
+ 24	pointer to URI string
+ 28	URI handle

This message is broadcast to indicate that the URI handler is attempting to process or check a URI.

If an application can process the URI it should acknowledge the message (with <URI_MProcessAck=>Msg_4e384>) and, if processing is requested, call <URI_RequestURI>.
